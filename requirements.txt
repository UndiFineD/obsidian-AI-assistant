# Production Requirements for Obsidian AI Assistant
# Core backend dependencies

# FastAPI and server
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
starlette>=0.27.0

# Machine Learning and AI (install with pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu)
torch>=2.0.0  # For Windows, use CPU-only wheels: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.23.0
sentence-transformers>=2.2.2
chromadb>=0.4.15
llama-cpp-python>=0.2.11
gpt4all>=2.0.2
transformers>=4.35.0
accelerate>=0.24.0
# bitsandbytes>=0.41.0  # Optional - for quantization support

# Hugging Face Hub
huggingface-hub>=0.17.0

# Web scraping and content processing
requests>=2.31.0
beautifulsoup4>=4.12.2
readability-lxml>=0.8.1
pypdf>=4.0.0

# Voice processing
vosk>=0.3.44

# Data processing and utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-core>=2.16.3
numpy==1.26.4
pandas>=2.0.0

# Cryptography for security
cryptography>=41.0.0
cffi>=1.16.0

# JWT handling
PyJWT>=2.8.0

# System monitoring and performance
psutil>=5.9.0

# Async HTTP client
aiohttp>=3.8.0
httpx>=0.25.0

# Async file operations
aiofiles>=23.2.0

# File type detection
python-magic>=0.4.27

# Configuration and data formats
PyYAML>=6.0.1

# Environment and configuration
python-multipart>=0.0.6  # For file uploads in FastAPI

# Additional core dependencies
Jinja2>=3.1.0  # Template engine (used by FastAPI and some ML libraries)
click>=8.1.0  # Command line interface utilities
tqdm>=4.65.0  # Progress bars for long-running operations
Pillow>=10.0.0  # Image processing support

# Additional utility packages for robust operation
chardet>=5.2.0  # Character encoding detection
lxml>=6.0.0  # XML/HTML parsing (used by BeautifulSoup)
certifi>=2023.0.0  # SSL certificate validation
urllib3>=2.0.0  # HTTP client library

# Note: PyTorch is intentionally excluded from automatic installs.
# Install CPU-only wheels explicitly if needed:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
